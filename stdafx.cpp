// stdafx.cpp: исходный файл, содержащий только стандартные включаемые модули
// OOP_laba1.pch будет предкомпилированным заголовком
// stdafx.obj будет содержать предварительно откомпилированные сведения о типе

#include "stdafx.h"

// TODO: Установите ссылки на любые требующиеся дополнительные заголовки в файле STDAFX.H
// , а не в данном файле

/*Rectangle n[3];

	for (int i = 0; i < 3; i++)
	{

		n[i].set();
		int a = n[i].area();
		cout << "площадь прямоугольника номер " << i + 1 << " равна " << a << endl;
		int p = n[i].perimeter();
		cout << "периметр прямоугольника номер " << i + 1 << " равен " << p << endl;
		cout << endl;

	}

	int maxa = n[0].area();
	int mina = n[0].area();
	int maxp = n[0].perimeter();
	int minp = n[0].perimeter();
	int xa = 0, na = 0, xp = 0, np = 0;
	for (int i = 0; i < 3; i++)
	{
		if (maxa < n[i].area())
		{
			maxa = n[i].area();
			xa = i;
		}
		if (mina > n[i].area())
		{
			mina = n[i].area();
			na = i;
		}
		if (maxp < n[i].perimeter())
		{
			maxp = n[i].perimeter();
			xp = i;
		}
		if (minp > n[i].perimeter())
		{
			minp = n[i].perimeter();
			np = i;
		}
	}
	cout << "максимальная площадь " << maxa << " у фигуры под номером " << xa + 1 << endl;
	cout << "минимальная площадь " << mina << " у фигуры под номером " << na + 1 << endl;
	cout << "максимальный периметр " << maxp << " у фигуры под номером " << xp + 1 << endl;
	cout << "минимальный периметр " << minp << " у фигуры под номером " << np + 1 << endl;

	*/